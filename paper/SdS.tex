\documentclass[
 aps,
 prd,
 reprint,
 superscriptaddress,
 nofootinbib,
 longbibliography
]{revtex4-2}

\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{mathrsfs}
\usepackage[hidelinks]{hyperref}

\newcommand{\scri}{\mathscr{I}}
\newcommand{\hc}{\mathcal{H}_{c}^{+}}
\newcommand{\hb}{\mathcal{H}_{b}^{+}}

\begin{document}

\title{Artificial cosmology as a regulator for hyperboloidal black-hole
evolution: Controlled recovery of Schwarzschild waveforms}

\author{Govind Arun Kumar}
\email{govind02@umd.edu}
\affiliation{Institute for Physical Science and Technology, University of
Maryland, College Park, Maryland 20742, USA}

\author{An\i l Zengino\u{g}lu}
\email{anil@umd.edu}
\affiliation{Institute for Physical Science and Technology, University of
Maryland, College Park, Maryland 20742, USA}

\date{\today}

\begin{abstract}
Conformal compactification of asymptotically flat evolution equations can
introduce formally singular terms at future null infinity.  Misner proposed
regulating those terms with a small positive cosmological constant, replacing
null infinity by a cosmological horizon where the conformal factor remains
nonzero.  We give a quantitative black-hole proof of principle using scalar
waves on Schwarzschild--de Sitter spacetime.  A useful regulator is not
specified by the metric alone: its foliation, compactification, initial-data
map, and waveform clock must approach a common Schwarzschild problem as the
cosmological length $L=\sqrt{3/\Lambda}$ grows.  We identify a bridge
foliation with this global limit and test the resulting family against an
independent Schwarzschild evolution.  Direct finite-$L$ waveform errors
decrease but remain observable dependent.  Two nested extrapolants formed
from $L/M=80,160,320,640$ recover the independent Schwarzschild waveform at
a conservative one-percent level for the cumulative, prompt-dominated
fixed-mode test and for a localized multimode response on its common
archived interval.  The latter provides a nontrivial angular and caustic
stress test.  We make no claim of uniform one-percent late-time accuracy.
Caustic-echo timing supplies supporting Schwarzschild--de Sitter physics and
is consistent with a local $\mathcal O((M/L)^2)$ correction and a larger
outer $\mathcal O(M/L)$ response, but its deterministic sensitivities do not
support precision fit coefficients.  The calculation establishes a scalar
regulator protocol and target accuracy for a future application to the full
Einstein equations; it is not itself an Einstein evolution.
\end{abstract}

\maketitle

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Introduction}
\label{sec:introduction}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Radiation from isolated systems has a  clean definition at future null
infinity, $\scri^+$.  Hyperboloidal evolution brings this clean definition
onto the computational domain through spacelike slices that approach
outgoing null hypersurfaces.  After spatial compactification places infinity
at a finite coordinate location, waves leave the grid through a
characteristic, pure outflow boundary.  Friedrich's conformal field equations regularize
the asymptotic problem \cite{friedrich1983cauchy}.  Applying scri fixing
directly to the Einstein equations exposes formally singular conformal
source terms whose limits must preserve the constraints in the chosen gauge
\cite{zenginoglu_hyperboloidal_2008,Zenginoglu:2008pw,
Moncrief:2008ie}.  Stable calculations now include Einstein evolution in
axisymmetry and spherical symmetry
\cite{Rinne:2009qx,Vano-Vinuales:2014koa,Vano-Vinuales:2017qij}.
Three-dimensional test fields and asymptotic model systems extend those
tests \cite{Zenginoglu:2010zm,Peterson:2023bha}.

Misner introduced artificial cosmology
\cite{misner_hyperboloidal_2006,misner_over_2006,misner_excising_2006}.
A small positive cosmological constant serves there as a regulator.
It replaces future null infinity with a distant cosmological horizon, so the
grid terminates in the pure-outflow region beyond it.  Black-hole excision
uses the same causal mechanism inside an event horizon.  Misner and
collaborators tested parts of this idea with Maxwell fields on de Sitter
backgrounds.  Scalar waves on Schwarzschild--de Sitter (SdS) provide a
black-hole model with a strong-field scattering region between the future
black-hole horizon $\hb$ and the future cosmological horizon $\hc$.

For $\Lambda>0$, the future conformal boundary is spacelike.  Its asymptotic
symmetries and radiative degrees of freedom differ from those at
$\scri^+$, as do the associated fluxes.  Ashtekar et al. developed this
structure for linear fields and derived a
positive-$\Lambda$ quadrupole formula
\cite{Ashtekar:2014zfa,Ashtekar:2015lla,Ashtekar:2015lxa}.  Here $\Lambda$
acts as a numerical regulator for scalar evolution.  We determine the
region over which that evolution approximates a specified asymptotically
flat solution, with the conformal boundary confining the comparison to a
finite spacetime region.

Regularity at $\hb$ and $\hc$ fixes the leading behavior of a bridge near
each horizon.  Its continuation into the expanding region beyond $\hc$
selects the asymptotic end.  Horizon crossings leave enough freedom for a
one-parameter family to approach a Cauchy-like Schwarzschild end.  The
regulator family includes its foliation and compactification along with the
data map and waveform clock.  Scalar observables remain invariant under
coordinate changes.  Our analysis tracks the limiting coordinates and the
conditioning they induce in the numerical equations.

Writing $\Lambda=3/L^2$, the SdS metric approaches Schwarzschild on every
fixed near-zone interval as $L/M\to\infty$.  Each finite value of $L$ keeps
the SdS global causal structure.  Schwarzschild fields develop inverse-power
tails
\cite{price1972nonspherical,zengin2008hyperboloidal,
Angelopoulos:2016wcv}.  SdS fields eventually display cosmological decay
\cite{Brady:1996za,brady_radiative_1999,Molina:2003dc}.  A finite-$L$
calculation approximates the asymptotically flat waveform over a finite
interval.  Cosmological decay sets its later behavior.

Using the bridge constructions and conformal geometry developed in
Ref.~\cite{zenginoglu_bridge_2025}, we form a one-parameter regulator family
on a fixed compact domain.  Analytic large-$L$ limits follow the slices to
their asymptotic ends.  Retarded-time offsets set a common clock, and the
compactified coefficients expose the conditioning of each choice.  We then
evolve a regular first-order scalar system in the minimal gauge and compare
each finite-$L$ waveform with an independently evolved Schwarzschild
reference without fitting a relative time translation.

The paper has two goals.  The primary goal is to test Misner's proposal as a
controlled regulator: we determine both the direct finite-$L$ error and the
accuracy of an $L\to\infty$ extrapolation.  A fixed pure-$\ell=2$ datum tests
the waveform limit cleanly, while a localized source excites many angular
modes and stresses the construction through caustics.  The secondary goal is
to study physical finite-$\Lambda$ effects, including caustic echoes and the
eventual departure from Schwarzschild late-time behavior.  Those effects
support the regulator analysis but do not constitute a second headline.

Section~\ref{sec:bridges} gives the global foliation criterion and identifies
the regulator gauge.  Section~\ref{sec:scalar} defines the scalar model,
matched data, clock, numerical methods, and comparison measures.
Section~\ref{sec:results} presents direct and extrapolated Schwarzschild
recovery followed by the localized-source test and supporting SdS physics.
The appendices collect convergence, sensitivity, and reproducibility details.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Bridge foliations and the regulator limit}
\label{sec:bridges}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Schwarzschild--de Sitter static patch}

The SdS metric in static coordinates is
\begin{align}
 ds^2&=-f(r)dt^2+\frac{dr^2}{f(r)}+r^2d\omega^2,
 \label{eq:sds_metric}\\
 f(r)&=1-\frac{2M}{r}-\frac{r^2}{L^2},
 \qquad d\omega^2=d\theta^2+\sin^2\theta\,d\varphi^2.
 \label{eq:f}
\end{align}
We use geometrized units $G_{\rm N}=c=1$.  The simulations set $M=1$ and
restore factors of $M$ in reported coordinates and dimensionless
diagnostics.
For $0<M<L/(3\sqrt{3})$, $f$ has a black-hole root $r_b$, a
cosmological root $r_c$, and a negative root $r_u=-(r_b+r_c)$.  They obey
\begin{align}
 L^2&=r_b^2+r_br_c+r_c^2,
 &2ML^2&=r_br_c(r_b+r_c),
 \label{eq:root_relations}\\
 f(r)&=\frac{(r-r_b)(r_c-r)(r-r_u)}{L^2r}.
 \label{eq:f_factorized}
\end{align}
For $i=b,c$, the positive surface gravities follow from
Eq.~\eqref{eq:kappa}.  The partial-fraction formulas use the positive
algebraic coefficient $\kappa_u:=|f'(r_u)|/2$ at the negative root:
\begin{equation}
 \kappa_i=\frac12\left|f'(r_i)\right|
 =\frac12\left|\frac{1}{r_i}-\frac{3r_i}{L^2}\right|,
 \qquad i\in\{b,c,u\}.
 \label{eq:kappa}
\end{equation}
With an arbitrary reference radius $r_0\in(r_b,r_c)$, the normalized
tortoise coordinate is
\begin{align}
r_{*,L}(r)={}&\frac{1}{2\kappa_b}
 \ln\frac{r-r_b}{r_0-r_b}
-\frac{1}{2\kappa_c}
 \ln\frac{r_c-r}{r_c-r_0}
\notag\\
&+\frac{1}{2\kappa_u}
 \ln\frac{r-r_u}{r_0-r_u}.
\label{eq:tortoise}
\end{align}
The tortoise coordinate tends to $-\infty$ at $r_b$ and to $+\infty$ at
$r_c$.  Its flat limit has
\begin{equation}
 r_b\to2M,\qquad r_c=L-M+\mathcal{O}(M^2/L),
 \label{eq:horizon_flat_limit}
\end{equation}
and the static patch expands to the Schwarzschild exterior.

\subsection{The global criterion beyond the horizons}

We introduce a stationary height function,
\begin{equation}
 \tau=t+h(r),\qquad B(r):=f(r)h'(r),
 \label{eq:height}
\end{equation}
where $B$ is the boost relative to $r_*$.  For a radial coordinate
$\rho=g(r)$ and $G=d\rho/dr$, the metric becomes
\begin{equation}
 ds^2=-f\,d\tau^2+\frac{2B}{G}\,d\tau d\rho
 +\frac{1-B^2}{fG^2}\,d\rho^2+r^2d\omega^2.
 \label{eq:transformed_metric}
\end{equation}
A future-directed spacelike bridge within the static patch satisfies
\begin{equation}
 |B|<1,\qquad B(r_b)=+1,\qquad B(r_c)=-1,
\label{eq:boost_conditions}
\end{equation}
where $(1-B^2)/f$ must tend to a finite positive nonzero value at each
horizon for a radial map with finite nonzero endpoint values of $G$.
Equivalently, $h\sim+r_*$ at $\hb$ and $h\sim-r_*$ at $\hc$.  The leaves
are null-transverse and the transformed coefficients have regular horizon
limits \cite{zenginoglu_bridge_2025}.  Beyond $\hc$, where $f<0$, spacelike
continuation on the future branch requires $B<-1$.  The value $B(r_c)=-1$
supplies the local crossing data.  The behavior of $B$ beyond that point
determines how the leaves approach the spacelike future boundary.  This
expanding region supports forward and scattering problems
\cite{Schlue:2012pq,Bernhardt:2024hfs}.

Two heights normalized by $h(r_0)=0$ carry the same endpoint data:
\begin{align}
h_{\rm min}(r)={}&
\frac{1}{2\kappa_b}\ln\left|\frac{r-r_b}{r_0-r_b}\right|
+\frac{1}{2\kappa_c}\ln\left|\frac{r_c-r}{r_c-r_0}\right|,
\label{eq:minimum_height}\\
h_{\rm mg}(r)={}&
\frac{1}{2\kappa_b}
\ln\left|\frac{(r-r_b)r_0}{(r_0-r_b)r}\right|
+\frac{1}{2\kappa_c}
\ln\left|\frac{(r_c-r)r_0}{(r_c-r_0)r}\right|
\notag\\
&-\frac{1}{2\kappa_u}
\ln\left|\frac{(r-r_u)r_0}{(r_0-r_u)r}\right|.
\label{eq:minimal_height}
\end{align}
Local horizon regularity supplies the two logarithms in the minimum height.
The minimal gauge adds the third-root term and radial factors
\cite{PanossoMacedo:2023qzp,zenginoglu_bridge_2025}.  Equation
\eqref{eq:boost_conditions} holds for both families.  Figure
\ref{fig:bridge_foliations} follows their leaves beyond $\hc$.  As
$L\to\infty$, the minimum-height family closes toward a Cauchy-like
asymptotic end.  Minimal-gauge leaves open toward Schwarzschild $\scri^+$,
as seen in the two conformal diagrams.

\begin{figure*}[t]
 \centering
 \includegraphics[width=0.49\textwidth]{figs/minimalAnil.pdf}
 \includegraphics[width=0.49\textwidth]{figs/minimalRodrigo.pdf}
 \caption{Conformal diagrams of two SdS bridge families from \cite{zenginoglu_bridge_2025}.  Each family crosses the
 future horizons regularly.  The minimum-height leaves
 in the left panel close toward a Cauchy-like end as $L$ grows.  In the
 right panel, minimal-gauge leaves open toward Schwarzschild $\scri^+$.
 Solid and dashed curves belong to the separate horizon-regular conformal
 charts used to construct the diagrams.}
 \label{fig:bridge_foliations}
\end{figure*}

For a bridge normalized by $h(r_0)=0$ and $r_{*,L}(r_0)=0$, the boost gives
the clock diagnostic
\begin{equation}
 q_B(L;r_0):=\lim_{r\to r_c}(h+r_{*,L})
 =\int_{r_0}^{r_c}\frac{1+B}{f}\,dr.
 \label{eq:general_q}
\end{equation}
The integrand is regular at $r_c$.  A divergent $q_B$ sends the origin of
the source clock to infinity as the outer null surface recedes.  A finite
$q_B$ supplies the required clock limit.

\subsection{Six bridges and a fixed compactification}

In addition to Eqs.~\eqref{eq:minimum_height} and
\eqref{eq:minimal_height}, we consider four boosts specified directly:
\begin{align}
B_{\rm lin}&=-1+2\frac{r_c-r}{r_c-r_b},
\label{eq:linear_boost}\\
B_{\rm fl}&=-1+2\frac{r_c-r}{r_c-r_b}\frac{r_b^2}{r^2},
\label{eq:flat_linear_boost}\\
B_{\rm Mav}&=-\frac{1-3M/r}{\sqrt{1-9M^2\Lambda}}
 \sqrt{1+\frac{6M}{r}},
\label{eq:mav_boost}\\
B_{\rm sr}&=-\gamma r+\frac{\beta}{r^2},
\label{eq:slow_roll_boost}\\
\gamma&=\frac{r_c^2+r_b^2}{r_c^3-r_b^3},\qquad
\beta=\frac{r_c^2r_b^2(r_c+r_b)}{r_c^3-r_b^3}.
\label{eq:slow_roll_parameters}
\end{align}
The Mavrogiannis boost is motivated by wave estimates on SdS and turns at the
photon sphere $r=3M$
\cite{mavrogiannis2024quasilinear,mavrogiannis2023morawetz}.

At fixed areal radius, with $x=M/r$, the six boosts have the limits
\begin{equation}
\begin{array}{lcc}
\text{bridge}&B_0(r)&B_0(\infty)\\
\hline
\text{minimum height}&3x-\tfrac12&-\tfrac12\\
\text{minimal gauge}&-1+8x^2&-1\\
\text{linear}&+1&+1\\
\text{modified linear}&-1+8x^2&-1\\
\text{Mavrogiannis}&-(1-3x)\sqrt{1+6x}&-1\\
\text{slow roll}&4x^2&0
\end{array}
\label{eq:boost_limits}
\end{equation}
An outgoing Schwarzschild hyperboloidal slice necessarily has $B_0\to-1$ at
$r\to\infty$.  With the compactification \eqref{eq:compactification}, a
regular null-infinity limit further requires
$1+B_0=\mathcal{O}(M^2/r^2)$ with a finite positive leading coefficient.
The minimum-height, linear, and slow-roll limits fall outside this
asymptotic-nullness condition.  Minimal, modified-linear, and Mavrogiannis
slices satisfy it.  A full regulator limit also requires uniform
compactified coefficients through the expanding region.  For the
modified-linear boost, an outer layer breaks uniformity near $\rho=1$.

We place both horizons at fixed coordinate locations using
\begin{align}
 \rho&=\frac{1-r_b/r}{1-r_b/r_c},
 &r(\rho)&=\frac{r_b}{1-(1-r_b/r_c)\rho},
 \label{eq:compactification}\\
 G(r)&=\frac{d\rho}{dr}
 =\frac{r_b}{(1-r_b/r_c)r^2}.
 \label{eq:G}
\end{align}
The black-hole and cosmological horizons are at $\rho=0$ and $1$,
respectively.  At fixed $r$,
\begin{equation}
 \rho\to\rho_0:=1-\frac{2M}{r},
 \label{eq:rho_limit}
\end{equation}
which is also the compactification used by the independent Schwarzschild
solver.

The corresponding dimensionless spacetime conformal factor may be chosen as
\begin{equation}
 \Omega_L:=\frac{r_b}{r}
 =1-\left(1-\frac{r_b}{r_c}\right)\rho .
 \label{eq:conformal_factor}
\end{equation}
At every finite $L$ the outer endpoint is the cosmological horizon and
\begin{equation}
 \Omega_L(1)=\frac{r_b}{r_c}>0.
 \label{eq:finite_L_conformal_factor}
\end{equation}
In the Schwarzschild limit,
$\Omega_L\to\Omega_0=1-\rho$ and $\Omega_0(1)=0$, so the same endpoint becomes
$\scri^+$.  Equation~\eqref{eq:finite_L_conformal_factor} is the regulator
mechanism in Misner's proposal: terms that are formally singular because the
asymptotically flat conformal factor vanishes would be evaluated at nonzero
$\Omega_L$ in a finite-$L$ Einstein problem.  The question addressed below is
whether the associated finite-$L$ observables can nevertheless be used to
recover the $\Omega_0=0$ problem in a controlled limit.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Scalar evolution and comparison protocol}
\label{sec:scalar}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Regular first-order system}

For a minimally coupled massless scalar field, write
\begin{equation}
 \Phi(t,r,\theta,\varphi)=\frac{1}{r}
 \sum_{\ell m}u_{\ell m}(t,r)Y_{\ell m}(\theta,\varphi).
 \label{eq:scalar_decomposition}
\end{equation}
Each mode satisfies
\begin{align}
 \left(-\partial_t^2+\partial_{r_*}^2-V_\ell\right)u&=0,
 \label{eq:wave_equation}\\
 V_\ell&=f\left[\frac{\ell(\ell+1)}{r^2}
 +\frac{f'(r)}{r}\right].
 \label{eq:scalar_potential}
\end{align}
We suppress the mode labels below and introduce
\begin{equation}
 p:=\frac{d\rho}{dr_*}=fG,\qquad
 A:=\frac{p}{1-B^2},\qquad P:=\frac{V_\ell}{p},
 \label{eq:coefficients}
\end{equation}
The first-order fields are
\begin{equation}
 \psi:=\partial_\rho u,\qquad
 \pi:=\frac{1-B^2}{p}\partial_\tau u-B\psi.
 \label{eq:first_order_variables}
\end{equation}
The evolution system is
\begin{align}
 \partial_\tau u&=A(B\psi+\pi),
 \label{eq:evolve_u}\\
 \partial_\tau\psi&=\partial_\rho\!\left[A(B\psi+\pi)\right],
 \label{eq:evolve_psi}\\
 \partial_\tau\pi&=\partial_\rho\!\left[A(\psi+B\pi)\right]-Pu.
 \label{eq:evolve_pi}
\end{align}
We monitor the reduction constraint
\begin{equation}
 C:=\psi-\partial_\rho u.
 \label{eq:constraint}
\end{equation}

The radial characteristic speeds are
\begin{align}
 c_-&=-A(1+B)=-\frac{p}{1-B},
\notag\\
 c_+&= A(1-B)= \frac{p}{1+B}.
\label{eq:characteristic_speeds}
\end{align}
The endpoint speeds are $(c_+,c_-)=(0,c_-<0)$ at $\rho=0$ and
$(c_+,c_-)=(c_+>0,0)$ at $\rho=1$.  The characteristic flow determines all
endpoint values.  For a
simple crossing with $B'(r_h)\ne0$, analytic cancellation gives
\begin{equation}
 A_h=\frac{f'(r_h)G(r_h)}{-2s_hB'(r_h)},\qquad
 s_b=+1,\quad s_c=-1.
 \label{eq:A_endpoint}
\end{equation}
We cancel every height-function pole algebraically and assign
Eq.~\eqref{eq:A_endpoint} directly at each endpoint.

The evolution domain ends on $\hc$.  The analytic continuation of the height
function beyond that horizon is nevertheless essential: it selects the
asymptotic end approached by the family as $L/M\to\infty$.  The finite-$L$
evolutions test the regular crossing coefficients and extract the outer
waveform on the cosmological horizon.

For the minimal gauge, the limiting Schwarzschild coefficients are
\begin{align}
 B_0&=-1+2(1-\rho_0)^2,
 &A_0&=\frac{1}{8M(2-\rho_0)},
 \label{eq:schwarz_coefficients}\\
 P_0&=\frac{\ell(\ell+1)+(1-\rho_0)}{2M}.
 \label{eq:schwarz_potential}
\end{align}
The Schwarzschild code implements these expressions independently of the
finite-$L$ code.

\subsection{Numerical method}

Equations~\eqref{eq:evolve_u}--\eqref{eq:evolve_pi} are solved in double
precision by two implementations with independent radial discretizations and
time integrators.  The pure $\ell=2$ production sequence uses Dedalus~3
\cite{burns_dedalus_2020} with a Chebyshev-$T$
collocation basis on $0\leq\rho\leq1$ and the second-order RK222 stepper;
values at finite radius observers come from the spectral interpolation
operator.  The localized source production sequence uses a separate code in
which one radial response for every distinct retained $\ell$ is carried on a
uniform $\rho$ grid with a shared eighth-order centered stencil, matched
one-sided endpoint stencils,
and classical RK4 with staged source evaluation.  The analytic geometry,
physical source prescription, modal reconstruction, and diagnostics are
shared deliberately; the spatial discretizations and time integrators are
not.  A separate sourced configuration evolved by both implementations gives
the cross-code check reported in Appendix~\ref{app:convergence}.

Each family is refined on its own three-level ladder.  The fixed-mode ladder
is $(N,\Delta\tau/M)=(384,0.005)$, $(512,0.00375)$, $(768,0.0025)$, evolved
to $\tau=200M$ with boundary output every $0.03M$.  The localized source
ladder is
$(N_r,\Delta\tau/M,\ell_{\max})=(1024,0.001,42)$, $(1536,1/1500,46)$,
$(2048,0.0005,50)$, evolved to $\tau=60M$ with output at the stepping
cadence and observers at $r=8M$, $r=12M$, and the outer boundary.  Radial
resolution, time step, and angular truncation are refined together, so a
level-to-level change measures the combined refinement sensitivity of a case
instead of isolating one operator.  Every finite $L$ case has a
Schwarzschild reference evolved at the same level by the independent
implementation of
Eqs.~\eqref{eq:schwarz_coefficients}--\eqref{eq:schwarz_potential}.  The
$\ell=2$ angular index is exact for the fixed-mode family, so $\ell_{\max}$
enters only through the localized source.

Discretization error is kept apart from the estimator, window, and cadence
sensitivities of the timing analysis, and from the physical dependence of
the response on the source width.  Appendix~\ref{app:convergence} reports
these separately and never combines them into a single error bar.

The raw archives, case commands, code identities, and derived artifacts of
the production campaign are recorded in a hash-verified manifest, and
Appendix~\ref{app:reproducibility} gives the identifiers.

\subsection{Geometrically matched data and clocks}
\label{sec:protocol}

\paragraph{Fixed-window data.}
For the finite-time flat limit, the reduced field is the same smooth compact
profile in areal radius on every background:
\begin{equation}
x=\frac{r-4M}{1.5M},
\label{eq:displacement_x}
\end{equation}
\begin{equation}
u(0,r)=
\begin{cases}
\displaystyle\exp\left(1-\frac{1}{1-x^2}\right),& |x|<1,\\
0,&|x|\geq1.
\end{cases}
\label{eq:displacement_data}
\end{equation}
The support is $2.5M<r<5.5M$.  We initialize
$\psi=D_\rho u$, where $D_\rho$ is the Chebyshev differentiation operator in
the finite spectral representation, and
\begin{equation}
 \pi=-B\psi,
\label{eq:killing_stationary_data}
\end{equation}
so that $\partial_\tau u=\partial_tu=0$ initially.  The data are momentarily
stationary with respect to the static Killing field and retain a nonzero
normal derivative on the bridge.  Their support in areal radius stays fixed
across $L$.

\paragraph{Tail data.}
For the late-time study,
\begin{equation}
 u=\psi=0,\qquad \partial_\tau u=G_{\rm v}(r),\qquad
 \pi=\frac{G_{\rm v}(r)}{A},
\label{eq:velocity_data}
\end{equation}
where $x_{\rm v}=(r-6M)/(3M)$ and the peak-normalized $C^\infty$ profile is
\begin{equation}
 G_{\rm v}(r)=
 \begin{cases}
 \displaystyle\exp\left(1-\frac{1}{1-x_{\rm v}^2}\right),
   & |x_{\rm v}|<1,\\
 0,& |x_{\rm v}|\geq1.
 \end{cases}
\label{eq:velocity_profile}
\end{equation}
Its support is $3M<r<9M$.  Dividing by the background-dependent $A$ fixes
the prescribed Killing-time derivative profile across backgrounds.  The
momentum array changes with $A$.  Since the $\tau=0$ bridge hypersurfaces
vary with $L$, Eqs.~\eqref{eq:displacement_data}--\eqref{eq:velocity_profile}
define a geometric map among their data.

\paragraph{Retarded time.}
We set $r_0=4M$ and normalize
\begin{equation}
 h_L(r_0)=0,\qquad r_{*,L}(r_0)=0.
\end{equation}
For the minimal gauge, the cosmological-horizon logarithms in
$h_L+r_{*,L}$ cancel exactly, giving
\begin{align}
q_L={}&\frac{1}{2\kappa_b}
\ln\frac{(r_c-r_b)^2r_0}{r_c(r_0-r_b)^2}
\notag\\
&+\left(\frac{1}{2\kappa_u}-\frac{1}{2\kappa_c}\right)
\ln\frac{r_c}{r_0}.
\label{eq:q_L}
\end{align}
The independent Schwarzschild value is
\begin{equation}
 q_0=4M\ln\frac{r_0}{r_0-2M}=4M\ln2.
 \label{eq:q_zero}
\end{equation}
Waveforms are compared at the geometric retarded time
\begin{equation}
 U=\tau-q_L.
\label{eq:retarded_time}
\end{equation}
At $\hc$ this is the outgoing retarded-time coordinate.  The same
normalization reaches Schwarzschild $\scri^+$ in the limit.  At finite
radius, $U$ is a stationary clock normalized at the outer boundary.  Its
relation to the local outgoing coordinate is
\begin{equation}
 U-(t-r_{*,L})=h_L(r)+r_{*,L}(r)-q_L.
\label{eq:finite_radius_clock}
\end{equation}
Since the constant drops out of a decay rate, reported transition times keep
the geometric origin in Eq.~\eqref{eq:retarded_time}.

\subsection{Localized source and regulator observables}
\label{sec:localized_protocol}

The fixed-mode calculation isolates the radial and temporal regulator limit.
A second family uses a normalized source localized in radius, time, and
angle.  Its spherical-harmonic decomposition excites a multimode response,
including propagation through caustics.  The physical source prescription is
held fixed across the SdS and Schwarzschild backgrounds; source width is
therefore a property of the chosen response, not a numerical error bar.
This family solves
\begin{equation}
 \Box_g\Phi=S,
 \qquad \Phi|_{\tau=0}=\partial_\tau\Phi|_{\tau=0}=0.
 \label{eq:sourced_wave_equation}
\end{equation}

Let $b(x)=\exp\left[1-1/(1-x^2)\right]$ for $|x|<1$ and $b=0$ otherwise, the
profile already used in Eqs.~\eqref{eq:displacement_data} and
\eqref{eq:velocity_profile}, and let $\gamma$ be the angle from the emitter
direction $(\theta_s,\varphi_s)=(\pi/2,0)$.  The source is the product
\begin{equation}
 S=\mathcal A\,T(t)\,R(r)\,\mathcal P_\kappa(\gamma),
 \label{eq:source_product}
\end{equation}
\begin{align}
 T(t)&=\frac{b\left[(t-t_s)/\sigma_t\right]}{\sigma_t\beta_0},
 &\beta_0&=\int_{-1}^{1}b(x)\,dx,
 \label{eq:source_time}\\
 R(r)&=\frac{b\left[(r-r_s)/\sigma_r\right]}{\beta_2},
 &\beta_2&=\int b\!\left[\frac{r-r_s}{\sigma_r}\right]r^2dr,
 \label{eq:source_radial}\\
 \mathcal P_\kappa(\gamma)&=\frac{\kappa\,e^{\kappa(\cos\gamma-1)}}
 {2\pi\left(1-e^{-2\kappa}\right)}.
 \label{eq:source_angular}
\end{align}
Each factor carries unit integral in its own variable, so
$\int\sqrt{-g}\,S\,d^4x=\mathcal A$ on every background and the strength of
the emitter does not drift with its width.  The production values are
$r_s=6M$, $\sigma_r=0.75M$, $t_s=30M$, $\sigma_t=2M$, and $\kappa=64$,
giving the characteristic angular scale $\kappa^{-1/2}=0.125$ radians and support
$5.25M<r<6.75M$ over $28M<t<32M$.  The
same numbers are used on SdS and on Schwarzschild.

The real-spherical-harmonic expansion of
Eq.~\eqref{eq:source_angular} is
\begin{equation}
 \mathcal P_\kappa(\gamma)
 =\sum_{\ell m}a_{\ell m}Y_{\ell m}(\theta,\varphi),
 \label{eq:source_angular_expansion}
\end{equation}
with constant angular coefficients
\begin{equation}
 a_{\ell m}=\frac{i_\ell(\kappa)}{i_0(\kappa)}
 Y_{\ell m}(\theta_s,\varphi_s),
 \label{eq:source_modal_amplitudes}
\end{equation}
with $i_\ell$ the modified spherical Bessel function.  Modes below
$10^{-13}$ of the largest amplitude are dropped, which for an equatorial
emitter removes exactly the orders forbidden by the reflection symmetries.
Because the radial and temporal factors are independent of $m$, the code
evolves one response $v_\ell$ for each distinct retained $\ell$:
\begin{equation}
 \left(-\partial_t^2+\partial_{r_*}^2-V_\ell\right)v_\ell
 =f r\,\mathcal A T(t)R(r).
 \label{eq:sourced_radial_equation}
\end{equation}
For each response, define $\psi_\ell=\partial_\rho v_\ell$ and the
corresponding $\pi_\ell$ by Eq.~\eqref{eq:first_order_variables}.  The first
two equations of the first-order system are unchanged, while
Eq.~\eqref{eq:evolve_pi} becomes
\begin{equation}
 \partial_\tau\pi_\ell
 =\partial_\rho\!\left[A(\psi_\ell+B\pi_\ell)\right]-P_\ell v_\ell
 -\frac{r}{G}\,\mathcal A
 T\!\left(\tau-h_L(r)\right)R(r).
 \label{eq:sourced_pi_equation}
\end{equation}
The explicit evaluation at the Killing time $t=\tau-h_L(r)$ makes the same
physical emitter act on every background.  All responses start from
$v_\ell=\psi_\ell=\pi_\ell=0$, so the source generates the entire field.
The truncation $\ell_{\max}$ is set by the refinement ladder and enters only
through the omitted angular power.

The modal coefficients are reconstructed as
$u_{\ell m}=a_{\ell m}v_\ell$.  The compact production archives store
$v_\ell(U)$ once per distinct $\ell$, together with the catalogue of
$a_{\ell m}$, rather than duplicating an identical radial response for every
$m$.  A directional waveform, including a caustic direction, is therefore
\begin{equation}
 u(U,\theta,\varphi)
 =\sum_{\ell m}a_{\ell m}v_\ell(U)Y_{\ell m}(\theta,\varphi)
 \label{eq:angular_reconstruction}
\end{equation}
at a given observer.  Extraction uses $r=8M$, $r=12M$,
and the outer boundary, with the retarded time of
Eq.~\eqref{eq:retarded_time} in every case.

At the outer endpoint the fixed-mode observable is the reduced field
$W_L(U):=u_2(U,\rho=1)$ on $\hc$; its Schwarzschild counterpart
$W_0(U):=u_2(U,\rho_0=1)$ is evaluated at $\scri^+$.  Thus $W$ is the
relevant spherical-harmonic coefficient of the radiation-scaled field
$r\Phi$, not the full field $\Phi$.  For a common time interval
$\mathcal J$, the direct fixed-mode waveform error is
\begin{equation}
 E_2(L;\mathcal J)=
 \frac{\|W_L-W_0\|_{L^2(\mathcal J)}}
      {\|W_0\|_{L^2(\mathcal J)}},
 \label{eq:regulator_E2}
\end{equation}
where $W_0$ is computed independently.  No relative time translation is
fitted.  For the localized source, $W$ denotes the outer-boundary modal
collection $\{u_{\ell m}\}$ and the primary comparison uses the
sphere-integrated Parseval norm
\begin{equation}
 \|W\|_{\mathcal J,S^2}^2=
 \int_{\mathcal J}dU\sum_{\ell m}|u_{\ell m}(U)|^2.
 \label{eq:parseval_norm}
\end{equation}
and the corresponding relative error
\begin{equation}
 E_{2,S^2}(L;\mathcal J)=
 \frac{\|W_L-W_0\|_{\mathcal J,S^2}}
      {\|W_0\|_{\mathcal J,S^2}}.
 \label{eq:localized_regulator_E2}
\end{equation}
Assuming the finite-$L$ observable admits the tested expansion
\begin{equation}
 W_L=W_0+c_1\frac{M}{L}+c_2\left(\frac{M}{L}\right)^2
 +\mathcal O\!\left((M/L)^3\right),
 \label{eq:regulator_expansion}
\end{equation}
the three-level combination
\begin{equation}
 W_\infty^{(L)}=
 \frac{W_L-6W_{2L}+8W_{4L}}{3}
 \label{eq:nested_extrapolant}
\end{equation}
cancels the displayed finite-$L$ terms.  We test the extrapolation rather
than assume it by comparing the nested triples based at $L/M=80$ and $160$
with one another and with $W_0$.

The fixed-mode comparison uses three cumulative intervals,
$0\leq U/M\leq40$, $80$, and $160$, and the two disjoint intervals
$40\leq U/M\leq80$ and $80\leq U/M\leq160$.  The localized source comparison
uses the common archived interval $0.000411\leq U/M\leq57.2274$, which runs
from the first stored nonnegative sample to the last sample held by every
member of the family.  Stored samples are mapped onto one fixed grid in $U$
by linear interpolation before any norm is formed, and no relative time
translation is fitted at any stage.  Beside $E_2$, the fixed-mode tables
report $E_\infty$ normalized by the largest Schwarzschild amplitude in the
same interval, together with an amplitude ratio and a phase offset taken
from the zero lag analytic signal overlap.

Numerical scales are propagated through the same comparisons rather than
assigned afterward.  Let $R_q=X_q-Y_q$ be the residual waveform for a
comparison at refinement level $q$.  The directly observed medium-to-fine
change is
\begin{equation}
 \delta_{mf}=
 \frac{\|R_{\rm medium}-R_{\rm fine}\|}{\|W_{0,\rm fine}\|},
 \label{eq:paired_refinement_change}
\end{equation}
with either the fixed-mode or Parseval norm as appropriate.  It is a norm of
the change in the paired residual waveform, not
$|E_{2,\rm medium}-E_{2,\rm fine}|$.  The coarse-to-medium and
medium-to-fine changes determine an observed coupled order, and the
Richardson estimate follows from that order and the refinement ratio.  The
case-specific numerical scale used in the direct threshold statements is the
larger of $\delta_{mf}$ and the Richardson estimate.

Two properties of these intervals control how the numbers may be read.  The
cumulative norms are dominated by the prompt waveform: at $L/M=320$ the
value is $2.7161\%$ on $0\leq U/M\leq40$ and $2.7154\%$ on both longer
intervals, so extending the interval adds interval length without adding
comparable signal power.  Statements about the cumulative windows are
therefore statements about the prompt and early ringdown.  On the disjoint
late intervals the observed refinement change is not subdominant: it reaches
$1.21\%$ against a $2.41\%$ difference on $40\leq U/M\leq80$, and it exceeds
the difference itself on $80\leq U/M\leq160$.  Those intervals are reported
as diagnostics and carry no accuracy claim.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Results}
\label{sec:results}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Foliation classes and numerical conditioning}
\label{sec:foliation_results}

Local horizon regularity alone does not select a regulator gauge.  The
relevant criterion is the behavior of the same geometric construction as
$L/M$ grows under a fixed compactification.  We therefore compare the exact
large-$L$ limits and the resulting compactified evolution coefficients.

Figure~\ref{fig:foliation_limit} tests the stronger large-$L$ requirement
under the fixed compactification \eqref{eq:compactification}.  Panel (a)
plots the exact fixed-$r$ limits in Eq.~\eqref{eq:boost_limits}.  Panel (b)
evaluates Eq.~\eqref{eq:general_q} with $r_0=4M$.  The offset set
$\{q_{\rm min},q_{\rm lin},q_{\rm sr}\}$ diverges with $L$ according to
\begin{equation}
\begin{split}
 q_{\rm min}&=\tfrac12L\ln2+\mathcal O\!\left[M\ln(L/M)\right],\\
 q_{\rm lin}&=2L\ln2+\mathcal O\!\left[M\ln(L/M)\right],\\
 q_{\rm sr}&=L\ln2+\mathcal O\!\left[M\ln(L/M)\right].
\end{split}
\label{eq:offset_asymptotics}
\end{equation}
Across $20\leq L/M\leq160$, the minimal-gauge offset moves from
$2.4619M$ to $2.7243M$ on its way to $q_0=2.7726M$.  Modified-linear
slices approach the same $q_0$.  The finite Mavrogiannis offset belongs to
its distinct Schwarzschild hyperboloidal clock.

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{figs/foliation_flat_limit.pdf}
 \caption{Large-$L$ foliation and coefficient diagnostics.  (a) Fixed-$r$
 boost limits, with the outgoing Schwarzschild hyperboloidal target
 $1+B_0=\mathcal O(M^2/r^2)$ at $\scri^+$.  (b) Offsets from the source to
 the outer horizon,
 $q_B(L;4M)$.  The dotted line is the minimal-gauge Schwarzschild value.
 (c) $M\max_\rho|c_\pm|$ and (d) $M\min_\rho A$ on the compact interval.
 Visual encodings identify each family.  The minimal and modified-linear
 limits coincide in panel (a).}
 \label{fig:foliation_limit}
\end{figure*}

Panels (c) and (d) connect each asymptotic limit to the numerical
coefficients.  For the linear boost at $L/M=160$,
$M\max|c_\pm|=19.86$ and $M\min A=3.88\times10^{-5}$, with maximum speed
growing linearly in $L/M$.  The minimum of $MA$ falls as $(M/L)^2$ for the
minimum-height and slow-roll families.  Linear slices share this decay in
$A$.  All these values refer to the fixed compactification
\eqref{eq:compactification} and change under a new radial coordinate.  The
minimal and Mavrogiannis gauges keep coefficients of order $M^{-1}$ across
the compact domain.  At $L/M=160$ their
$(M\max|c_\pm|,M\min A)$ pairs are $(0.2498,0.06324)$ and
$(0.1499,0.07495)$.  For the modified-linear family, the desired pointwise
boost limit accompanies a nonuniform outer propagation coefficient:
\begin{equation}
 \lim_{L\to\infty}M A_{\rm fl}(1)=\frac14
 \ne
 \lim_{\rho\to1^-}\lim_{L\to\infty}M A_{\rm fl}(\rho)=\frac18.
\label{eq:modified_linear_layer}
\end{equation}
This mismatch occupies an outer layer that shrinks as $L$ grows.  The
endpoint has $Mc_{+,\rm fl}(1)\to1/2$.  Taking the Schwarzschild limit in the
interior gives $Mc_{+,0}(\scri^+)=1/4$.

We use the minimal gauge below because its hyperboloidal limit carries the
finite clock in Eq.~\eqref{eq:q_zero}.  Its coefficients stay bounded across
the compact domain.  An independent closed-form Schwarzschild
implementation supplies the reference.  The processed conditioning data and
the script that generates Fig.~\ref{fig:foliation_limit} are supplied with the
paper source; this analytic-coefficient study is distinct from the frozen
waveform campaign described in Appendix~\ref{app:reproducibility}.

\subsection{Direct finite-\texorpdfstring{$L$}{L} recovery}
\label{sec:direct_recovery}

The direct comparison answers the practical regulator question: how large
must $L/M$ be before a finite cosmological calculation approximates the
chosen Schwarzschild observable?  The answer depends on the observable and
accuracy threshold.  For the cumulative, prompt-dominated pure-$\ell=2$
sequence, agreement including the case-specific numerical margin first
reaches $5\%$ at $L/M=320$ and $2\%$ at $L/M=640$; direct $1\%$ agreement is
not reached through $L/M=640$.  These thresholds do not apply to the
localized-source response.

Figure~\ref{fig:flat_sequence} shows the mechanism behind those numbers.
The boundary waveforms at $L/M=20$ through $640$ carry the same prompt pulse
and ringdown morphology as the Schwarzschild reference.  Over the large-$L$
part of the sequence, the dominant residual amplitude falls approximately by
a factor of two per doubling of $L$, while its shape changes only weakly on
the displayed prompt interval.  This regular behavior motivates testing the
explicit expansion in Eq.~\eqref{eq:regulator_expansion}.

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{../results/regulator_production_v3/flat_waveform_sequence.pdf}
 \caption{Fixed-data, pure-$\ell=2$ sequence at the outer boundary.  Upper
 panel: waveforms for $L/M=20$ to $640$ against the independently evolved
 Schwarzschild reference.  Lower panel: the residual $W_L-W_{\rm Schw}$ on
 the same retarded time $U=\tau-q_L$, with no relative time translation
 fitted.  The residual amplitude falls with $L$ while its prompt-interval
 morphology changes only weakly.}
 \label{fig:flat_sequence}
\end{figure*}

Table~\ref{tab:flat_windows} lists the direct errors on every declared
interval.  The three cumulative entries at fixed $L$ differ only in their
fourth digit, which is the quantitative form of the prompt dominance in
Sec.~\ref{sec:localized_protocol}: the interval grows by a factor of four
while the norm does not move.  Figure~\ref{fig:flat_windows} shows the same
data against $L/M$ together with the case-specific numerical scale.  For the
cumulative intervals that scale sits far below the difference itself, so the
$5\%$ and $2\%$ crossings are properties of the regulator rather than of the
grid.  For $40\leq U/M\leq80$ and $80\leq U/M\leq160$ the two curves
approach and then cross, which is why those intervals appear in the table
as diagnostics.  No claim of uniform late-time accuracy follows from any of
these entries.

\begin{table*}[t]
 \caption{Direct regulator errors for the fixed-data, pure-$\ell=2$
 sequence.  $E_\infty$ is normalized by the largest Schwarzschild amplitude
 in the same interval.  The refinement column is the directly observed
 medium-to-fine paired-residual change in
 Eq.~\eqref{eq:paired_refinement_change}.  These thresholds apply to
 the pure $\ell=2$ family only and not to the localized source.}
\label{tab:flat_windows}
\begin{ruledtabular}
\begin{tabular}{rlrrrl}
$L/M$ & interval & $E_2$ (\%) & $E_\infty$ (\%) & observed refinement (\%) & status\\
\colrule
320 & $0\leq U/M\leq40$ & 2.7161 & 2.8160 & 0.0275 & quantitative\\
320 & $0\leq U/M\leq80$ & 2.7154 & 2.8160 & 0.0643 & quantitative\\
320 & $0\leq U/M\leq160$ & 2.7154 & 2.8160 & 0.1478 & quantitative\\
320 & $40\leq U/M\leq80$ & 2.4145 & 2.0113 & 1.2126 & diagnostic\\
320 & $80\leq U/M\leq160$ & 5.5766 & 1.8859 & 145.1606 & diagnostic\\
640 & $0\leq U/M\leq40$ & 1.3406 & 1.3937 & 0.0114 & quantitative\\
640 & $0\leq U/M\leq80$ & 1.3402 & 1.3937 & 0.0239 & quantitative\\
640 & $0\leq U/M\leq160$ & 1.3402 & 1.3937 & 0.0465 & quantitative\\
640 & $40\leq U/M\leq80$ & 1.1547 & 0.9708 & 0.4380 & diagnostic\\
640 & $80\leq U/M\leq160$ & 7.0146 & 2.0280 & 43.4837 & diagnostic\\
\end{tabular}
\end{ruledtabular}
\end{table*}

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{../results/regulator_production_v3/flat_window_errors.pdf}
 \caption{Direct error $E_2(L)$ for the fixed-data, pure-$\ell=2$ sequence on
 the cumulative intervals (left) and the disjoint intervals (right).  Solid
 curves with filled markers are the differences; dashed curves with crosses
 are the case-specific numerical scales from the same refinement ladder.
 The cumulative curves are prompt dominated and lie well above their
 numerical scale.  On the disjoint intervals the differences approach or
 cross their numerical scales, so those points are diagnostic.}
 \label{fig:flat_windows}
\end{figure*}

\subsection{Nested recovery of the Schwarzschild waveform}
\label{sec:extrapolated_recovery}

The primary regulator test is the agreement among
$W_\infty^{(80)}$, $W_\infty^{(160)}$, and the independent Schwarzschild
waveform.  Across the declared cumulative windows, the largest central
extrapolant residual is $0.0313\%$.  That small central value is not a
resolved accuracy measurement: the largest directly observed
medium-to-fine change of a derived extrapolant is $0.409\%$, while the
largest propagated Richardson fine-grid estimate is $0.119\%$.  We therefore
state the result conservatively as recovery of the tested Schwarzschild
waveform within $1\%$.

Figure~\ref{fig:nested} places both extrapolants against the independent
Schwarzschild waveform.  The overlapping triples are
$(80,160,320)$ and $(160,320,640)$ in units of $L/M$; they share the $160$
and $320$ calculations.  Their difference is therefore a correlated test of
the stability of Eq.~\eqref{eq:regulator_expansion} against omitted
higher-order terms, not an independent replication.  On the cumulative
windows, each extrapolant residual is smaller than the direct residual of its
largest-$L$ member by a factor between approximately $50$ and $125$.

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{../results/regulator_production_v3/nested_extrapolants.pdf}
 \caption{Nested extrapolants for the fixed-data, pure-$\ell=2$ sequence.
 Upper panel: $W_\infty^{(80)}$ and $W_\infty^{(160)}$ against the
 independently evolved Schwarzschild waveform.  Lower panel: the two
 residuals and the difference between the overlapping extrapolants, which is
 a correlated diagnostic of omitted higher-order terms.}
 \label{fig:nested}
\end{figure*}

Table~\ref{tab:extrapolants} gives the headline comparison on the cumulative
intervals.  The three columns are different quantities and are not
interchangeable: they give the fine-grid central residual, the paired
medium-to-fine change in Eq.~\eqref{eq:paired_refinement_change}, and the
Richardson estimate based on the observed three-level order.  Because the
refinement scale exceeds the central residual, the supported statement is
the conservative $1\%$ recovery quoted above.  The full audit, including the
diagnostic disjoint intervals where refinement dominates outright, is in
Appendix~\ref{app:convergence}.

\begin{table}[t]
 \caption{Nested extrapolants on the cumulative intervals, each of which
 begins at $U=0$ and ends at $U_{\max}$.  Central residual, observed
 medium-to-fine paired-residual change, and propagated Richardson estimate are
 distinct quantities and are not combined.}
\label{tab:extrapolants}
\begin{ruledtabular}
\begin{tabular}{lrrrr}
comparison & $U_{\max}/M$ & central & refinement & Richardson\\
 & & (\%) & (\%) & (\%)\\
\colrule
$W_\infty^{(80)}-W_{\rm Schw}$ & 40 & 0.0217 & 0.0469 & 0.0271\\
$W_\infty^{(80)}-W_{\rm Schw}$ & 80 & 0.0222 & 0.1124 & 0.0428\\
$W_\infty^{(80)}-W_{\rm Schw}$ & 160 & 0.0222 & 0.2666 & 0.0777\\
$W_\infty^{(160)}-W_{\rm Schw}$ & 40 & 0.0109 & 0.0190 & 0.0091\\
$W_\infty^{(160)}-W_{\rm Schw}$ & 80 & 0.0142 & 0.0523 & 0.0195\\
$W_\infty^{(160)}-W_{\rm Schw}$ & 160 & 0.0265 & 0.1430 & 0.0413\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & 40 & 0.0186 & 0.0657 & 0.0362\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & 80 & 0.0211 & 0.1643 & 0.0623\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & 160 & 0.0313 & 0.4092 & 0.1189\\
\end{tabular}
\end{ruledtabular}
\end{table}

\subsection{Localized multimode response}
\label{sec:localized_results}

The localized-source family tests the regulator beyond a single spherical
harmonic.  In the sphere-integrated norm \eqref{eq:parseval_norm}, the direct
differences are $E_{2,S^2}(320)=5.26710\%$ and
$E_{2,S^2}(640)=2.62569\%$.  The two extrapolants differ by $0.051706\%$ and each
agrees with the independent Schwarzschild response well inside the
conservative $1\%$ level.  These statements apply only to the common archived
interval, $0.000411\lesssim U/M\leq57.2274$, not to a complete late-time
signal.  Representative caustic directions are secondary diagnostics; the
sphere-integrated modal norm is the primary comparison.

The comparison is made in the norm \eqref{eq:parseval_norm} because the
source contains every retained $(\ell,m)$ pair, and Parseval orthogonality
turns the integral of $|u|^2$ over the sphere into a sum over the reconstructed
modal coefficients.  No angular direction is privileged: a single direction is one
weighted combination of the same coefficients, and choosing it would let the
reported error depend on where an observer happens to sit relative to the
caustic pattern.  The direction shown in Fig.~\ref{fig:localized} serves
only to show what the modal statement means in a waveform.

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{../results/regulator_production_v3/localized_source_regulator.pdf}
 \caption{Localized source on the common archived interval.  Top left:
 direct sphere-integrated error against $L/M$.  Top right: the three nested
 extrapolant comparisons.  Bottom left: instantaneous $L^2(S^2)$ norms of
 the Schwarzschild response and of the residuals.  Bottom right: the
 secondary caustic direction $\gamma=\pi$, shown as a diagnostic and not as
 the primary comparison.}
 \label{fig:localized}
\end{figure*}

Table~\ref{tab:localized} collects the values.  The observed medium-to-fine
changes stay at or below the $10^{-4}\%$ level throughout, more than a factor
of $60$ below even the smallest difference in the table.  They are used only
to establish that refinement is subdominant, not to claim accuracy at those
digits.  The source contract is fixed while $L$
varies: the same $\mathcal A$, $r_s$, $\sigma_r$, $t_s$, $\sigma_t$, and
$\kappa$ of Eqs.~\eqref{eq:source_product}--\eqref{eq:source_angular} are
imposed on SdS and on Schwarzschild, so the width of the emitter is part of
the chosen physical problem and never enters as an error estimate.

\begin{table}[t]
 \caption{Localized source comparison in the sphere-integrated modal norm on
 the common archived interval $0.000411\leq U/M\leq57.2274$.  The interval
 ends where the archives end and is not a complete late-time signal.}
\label{tab:localized}
\begin{ruledtabular}
\begin{tabular}{lrr}
comparison & $E_{2,S^2}$ (\%) & observed refinement (\%)\\
\colrule
$W_{320}-W_{\rm Schw}$ & 5.26710 & 0.00001\\
$W_{640}-W_{\rm Schw}$ & 2.62569 & 0.00004\\
$W_\infty^{(80)}-W_{\rm Schw}$ & 0.05838 & 0.00003\\
$W_\infty^{(160)}-W_{\rm Schw}$ & 0.00672 & 0.00010\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & 0.05171 & 0.00010\\
\end{tabular}
\end{ruledtabular}
\end{table}

\subsection{Supporting Schwarzschild--de Sitter physics}
\label{sec:sds_physics}

Finite $L$ is not merely a numerical nuisance.  The localized response
contains caustic echoes whose arrival times, amplitudes, and phase structure
probe the global SdS geometry.  A shift in the direct-to-first-echo spacing,
denoted by $D_1$, is
consistent with a local correction of order $(M/L)^2$ at fixed areal radius
and a parametrically larger outer response approaching order $M/L$.  The
combined deterministic timing sensitivities do not meet any of the four
specified analysis criteria to which a target applies, however, so the
scaling is used as consistency evidence rather than as a precision
measurement.

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{../results/regulator_production_v3/D1_scaling.pdf}
 \caption{Absolute shift $|D_1|$ in the direct-to-first-echo spacing at
 $r=8M$, $r=12M$, and the outer boundary.  Bars are combined deterministic
 sensitivities, not statistical uncertainties.  Open markers are diagnostic
 points, where $|D_1|$ is not large compared with its own sensitivity.  The
 dashed curves are the sensitivity-weighted fits in
 Eq.~\eqref{eq:d1_scaling_models}, used only as scaling-consistency guides;
 their coefficients are not precision measurements.}
 \label{fig:d1}
\end{figure*}

At a given extraction radius, let $U_{\rm dir}$ denote the arrival of the
direct pulse reconstructed at $\gamma=0$ and let $U_{\rm echo}$ denote the
first caustic echo reconstructed at $\gamma=\pi$.  The primary arrival
estimator is the matched-template lag against the corresponding
Schwarzschild pulse.  We define
\begin{equation}
 D_1(r;L)=
 \left(U_{\rm echo}-U_{\rm dir}\right)_{\mathrm{SdS},L}
 -\left(U_{\rm echo}-U_{\rm dir}\right)_{\mathrm{Schw}},
 \label{eq:d1_definition}
\end{equation}
so any background-independent clock offset cancels.  A measurement enters the
scaling fit only when all four matched-template lag optimizations succeed
without saturating their $\pm4M$ lag bounds and $|D_1|$ is at least three
times its combined fixed-source deterministic sensitivity.  All values remain
archived and plotted, but this criterion marks both local $L/M=640$
measurements as diagnostic and excludes them from the fit.  The separate
$1M$ null-geodesic residual
criterion is used only to reject the earlier $L/M=12$ phase pairs in
Appendix~\ref{app:convergence}; it is not a production $D_1$ selection rule.

Figure~\ref{fig:d1} shows $|D_1|$ for the two local observers and the outer
boundary.  The local values are consistent with an $\mathcal O((M/L)^2)$
correction at fixed areal radius, and the outer values with a
parametrically larger response approaching $\mathcal O(M/L)$, which is what
the near zone and outer geometry respectively suggest.  The dashed curves are
fits to
\begin{align}
 \frac{D_{1,\mathrm{local}}}{M}
 &=a_2\left(\frac{M}{L}\right)^2
 +a_4\left(\frac{M}{L}\right)^4,\notag\\
 \frac{D_{1,\mathrm{outer}}}{M}
 &=b_1\frac{M}{L}+b_2\left(\frac{M}{L}\right)^2.
 \label{eq:d1_scaling_models}
\end{align}
Inverse squared deterministic sensitivity scales are used as numerical
weights, not as inverse statistical variances.  The fit curves are therefore
read only as consistency with the expected orders; their coefficients and
scaled residual sums have no precision or chi-squared interpretation.  The
local fits use $L/M=80$, $160$, and $320$, while the outer fit also includes
$L/M=640$.

A second finite $\Lambda$ effect sets the interval over which any finite $L$
calculation can follow Schwarzschild at all.  At $r=8M$ the $\ell=1$
waveform leaves the Schwarzschild decay at $U=115.9M$ for $L/M=80$ and
$133.5M$ for $L/M=160$.  These two medians are approximately fixed in units
of $M$ and are consistent with a departure scale tied to the black hole.
Entry into cosmological decay occurs at $\kappa_cU=2.83$ and $2.94$; its
approximate constancy in cosmological units is consistent with a scale tied
to $\kappa_c^{-1}$.  The transition interval consequently widens with $L$
from both sides.  Departure is not the end of a Price tail: the
independent Schwarzschild reference does not settle within $5\%$ of its own
$\ell=1$ power law until $U=220.3M$ at the same radius, so both finite $L$
solutions leave the Schwarzschild waveform before the Schwarzschild power
law is established.  A finite $L$ evolution of this kind contains a
transient that agrees with Schwarzschild, a departure from it, and then
cosmological decay, and not a power law plateau followed by a cosmological
tail.  Appendix~\ref{app:latetime} carries the rate estimator sweep and the
radius dependence.

\subsection{Scope of the demonstration}
\label{sec:scope}

The calculation tests the complete regulator protocol for a linear scalar
field on fixed black-hole backgrounds.  It does not evolve the full Einstein
equations and does not demonstrate regularization of their conformal source
terms.  Its contribution is a quantitative target and validation strategy:
choose a globally compatible regulator family, keep the physical comparison
contract fixed, measure direct finite-$L$ errors, and verify nested
extrapolation against an independent asymptotically flat reference.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Conclusions}
\label{sec:conclusions}

An artificial-cosmology regulator is a coherent limiting construction, not
only a choice of positive cosmological constant.  The metric, foliation,
compactification, initial-data map, and waveform clock must all select the
same asymptotically flat problem.  Local regularity at the cosmological
horizon does not ensure this limit: the continuation of the slices beyond
that horizon determines which Schwarzschild end is recovered.  The minimal
gauge used here has the required global end, a finite limiting clock, and
well-conditioned compactified evolution coefficients.

For the tested scalar observables, direct SdS waveforms approach their
Schwarzschild counterparts as $L/M$ grows, but a single finite-$L$ run is not
uniformly an effectively Schwarzschild calculation.  The two nested
extrapolants provide the more useful regulator protocol.  Their agreement
with one another and with an independent Schwarzschild calculation supports
controlled recovery at a conservative $1\%$ level for the cumulative
prompt-dominated fixed-mode test and for the localized multimode response on
its common archived interval.  No corresponding claim is made for the
unresolved disjoint late-time windows.

The localized response also exposes genuine finite-$\Lambda$ physics.
Caustic timing is consistent with a near-zone correction of order
$(M/L)^2$ and a slower outer response approaching order $M/L$, although
deterministic extraction sensitivities preclude precision coefficient
claims.  Together these calculations establish a reproducible scalar
black-hole proof of principle for Misner's proposal.  Applying the protocol
to the full Einstein equations remains the central future test, where the
formally singular conformal source terms and the coupled gauge and constraint
systems must be treated directly.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\appendix

\section{Numerical verification and deterministic sensitivities}
\label{app:convergence}

Both production families use the three-level ladders of
Sec.~\ref{sec:scalar}, and each waveform comparison in the main text is
formed at all three levels.  The observed refinement change is the
medium-to-fine norm of the paired residual-waveform difference in
Eq.~\eqref{eq:paired_refinement_change}; it is not the scalar difference of
two reported $E_2$ values.  The Richardson estimate uses the order observed
from the coarse, medium, and fine sequence together with the resolution ratio
to estimate the remaining fine-level scale under that order.  The two answer
different questions and are never added or averaged; where they disagree the
larger enters the direct threshold tests.

For the fixed-mode family the largest cumulative refinement change is
$0.1478\%$ at $L/M=320$ and $0.0465\%$ at $L/M=640$, with Richardson
estimates $0.0138\%$ and $0.0035\%$.  Derived extrapolants inherit the
refinement of all three members, which is why their observed changes reach
$0.409\%$ while their central residuals stay near $0.03\%$.
Table~\ref{tab:extrapolant_audit} carries the complete extrapolant audit.
On the disjoint intervals refinement dominates: central residuals of $0.096$
to $0.211\%$ on $40\leq U/M\leq80$ carry refinement changes of $1.02$ to
$3.14\%$, and on $80\leq U/M\leq160$ refinement exceeds the residual
outright, which is the sole reason those rows appear as diagnostics rather
than as late-time accuracy statements.  For the localized source family the
observed refinement changes stay at the $10^{-4}\%$ level on the archived
interval.

\begin{table*}[t]
 \caption{Complete nested extrapolant audit for the fixed-data, pure-$\ell=2$
 sequence.  The central residual, the directly observed medium-to-fine
 paired-residual change, and the propagated Richardson estimate are distinct
 quantities.  A row is diagnostic when refinement is not subdominant.}
\label{tab:extrapolant_audit}
\begin{ruledtabular}
\begin{tabular}{llrrrl}
comparison & interval & central residual (\%) & observed refinement (\%) & Richardson estimate (\%) & status\\
\colrule
$W_\infty^{(80)}-W_{\rm Schw}$ & $0\leq U/M\leq40$ & 0.0217 & 0.0469 & 0.0271 & $1\%$ test\\
$W_\infty^{(80)}-W_{\rm Schw}$ & $0\leq U/M\leq80$ & 0.0222 & 0.1124 & 0.0428 & $1\%$ test\\
$W_\infty^{(80)}-W_{\rm Schw}$ & $0\leq U/M\leq160$ & 0.0222 & 0.2666 & 0.0777 & $1\%$ test\\
$W_\infty^{(80)}-W_{\rm Schw}$ & $40\leq U/M\leq80$ & 0.0957 & 2.1322 & 0.7704 & diagnostic\\
$W_\infty^{(80)}-W_{\rm Schw}$ & $80\leq U/M\leq160$ & 0.8518 & 263.6959 & 73.7109 & diagnostic\\
$W_\infty^{(160)}-W_{\rm Schw}$ & $0\leq U/M\leq40$ & 0.0109 & 0.0190 & 0.0091 & $1\%$ test\\
$W_\infty^{(160)}-W_{\rm Schw}$ & $0\leq U/M\leq80$ & 0.0142 & 0.0523 & 0.0195 & $1\%$ test\\
$W_\infty^{(160)}-W_{\rm Schw}$ & $0\leq U/M\leq160$ & 0.0265 & 0.1430 & 0.0413 & $1\%$ test\\
$W_\infty^{(160)}-W_{\rm Schw}$ & $40\leq U/M\leq80$ & 0.1882 & 1.0164 & 0.3665 & diagnostic\\
$W_\infty^{(160)}-W_{\rm Schw}$ & $80\leq U/M\leq160$ & 24.3986 & 145.1404 & 39.9353 & diagnostic\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & $0\leq U/M\leq40$ & 0.0186 & 0.0657 & 0.0362 & $1\%$ test\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & $0\leq U/M\leq80$ & 0.0211 & 0.1643 & 0.0623 & $1\%$ test\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & $0\leq U/M\leq160$ & 0.0313 & 0.4092 & 0.1189 & $1\%$ test\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & $40\leq U/M\leq80$ & 0.2105 & 3.1441 & 1.1369 & diagnostic\\
$W_\infty^{(80)}-W_\infty^{(160)}$ & $80\leq U/M\leq160$ & 24.6156 & 408.7846 & 113.6462 & diagnostic\\
\end{tabular}
\end{ruledtabular}
\end{table*}

A sourced configuration has also been run through both implementations at
$L/M=80$ on Schwarzschild and SdS as a separate validation data set rather
than as part of either production sequence.  Across
those archives the largest sphere-integrated relative difference between the
two implementations is
$5.58\times10^{-4}$ and the largest individual arrival difference is
$4.14\times10^{-4}M$.  That comparison used the analytic envelope arrival on
both sides, whereas the $D_1$ analysis of Sec.~\ref{sec:sds_physics} uses
the matched template lag as its primary estimator.  The two are therefore
reported as separate checks: the cross implementation numbers validate the
sourced waveform and timing pipeline, and are not combined with the
matched template values as measurements of one observable.

Table~\ref{tab:timing} separates the deterministic timing sensitivities.
The PDE column comes from the paired refinement ladder; the estimator column
from the difference between matched template and analytic envelope arrivals;
the window column from four displacements and rescalings of the fixed
extraction windows by $0.5M$; and the cadence column from analysis grids at
$0.0005M$, $0.001M$, and $0.002M$.  The combined column is their linear sum,
which is a conservative deterministic bound and not a standard deviation, a
confidence interval, or a fit weight with a $\chi^2$ interpretation.  The
specified combined analysis targets fail at every entry, although the PDE
contributions alone would meet them.  Dependence on the source width is a
physical property of the chosen emitter, is available only from the
separate earlier fixed-background width study, and is excluded from this fixed
source target by construction.

\begin{table*}[t]
 \caption{Deterministic timing sensitivities in units of $M$.  The combined
 column is a conservative linear sum and not a statistical error.  None of
 the four target-bearing entries meets its specified analysis criterion,
 while every PDE contribution alone would.  The
 associated $D_1$ scaling is therefore used only as consistency evidence.}
\label{tab:timing}
\begin{ruledtabular}
\begin{tabular}{rlrrrrrr}
$L/M$ & observer & PDE & estimator & window & cadence & combined & target\\
\colrule
320 & $r=8M$ & 6.80e-08 & 3.24e-04 & 2.52e-05 & 3.04e-07 & 3.49e-04 & 1.0e-04\\
320 & $r=12M$ & 7.86e-07 & 7.83e-04 & 1.59e-05 & 4.47e-08 & 8.00e-04 & 1.0e-04\\
320 & outer & 3.83e-09 & 4.03e-03 & 2.45e-04 & 2.21e-06 & 4.28e-03 & 5.0e-04\\
640 & outer & 1.04e-08 & 2.11e-03 & 1.27e-04 & 1.56e-06 & 2.24e-03 & 5.0e-04\\
\end{tabular}
\end{ruledtabular}
\end{table*}

Two exclusions are recorded rather than corrected.  Both local $L/M=640$
timings are diagnostic because $|D_1|$ is not large compared with the
combined sensitivity of the same measurement.  All six generic angle phase
pairs at $L/M=12$ are excluded from quantitative phase analysis because at
least one extracted pulse in each pair differs from its null geodesic
arrival by more than the $1M$ tolerance, the largest residual being
$15.666M$.  No phase estimate is reported for those pairs, and their
rejection carries no physical interpretation.

\begin{figure*}[t]
 \centering
 \includegraphics[width=\textwidth]{../results/regulator_production_v3/D1_error_separation.pdf}
 \caption{Separation of the deterministic $D_1$ sensitivities at each
 observer.  The measured $|D_1|$ is shown with the discretization,
 estimator, window, and cadence contributions and with their conservative
 linear sum.  The estimator contribution dominates the sum at every length,
 which is what prevents a precision coefficient statement.}
 \label{fig:d1_errors}
\end{figure*}

\begin{figure}[t]
 \centering
 \includegraphics[width=\linewidth]{../results/regulator_production_v3/L12_phase_exclusion.pdf}
 \caption{Null geodesic residuals of the extracted pulses for the $L/M=12$
 generic angle pairs.  Every pair has at least one pulse outside the
 declared $1M$ tolerance, so all six are excluded from quantitative phase
 analysis.}
 \label{fig:l12}
\end{figure}

The checks above are what separate a reproducible central value from a
resolved accuracy.  Refinement changes and Richardson estimates are reported
separately throughout, and the deterministic extraction sensitivities are
neither statistical standard deviations nor fit weights with a chi-squared
interpretation.

\section{Late time departure from Schwarzschild}
\label{app:latetime}

The main text quotes the $r=8M$ transition because it is the cleanest
supporting case.  This separate crossover study treats the $\ell=1$ response
for $L/M=20$, $40$, $80$, and $160$ at $r=4M$, $8M$, $16M$, and $\hc$.
Finite-radius observations are compared with an independent Schwarzschild
run at the same areal radius, while $\hc$ is compared with Schwarzschild
$\scri^+$.  An additional $\ell=2$, $L/M=80$ calculation checks the
mode dependence.

Departure is the end of the last persistent interval, before cosmological
entry, on which the local decay rate agrees with the Schwarzschild reference.
Entry is the first persistent interval on which the rate agrees with the
expected SdS value.  The rate is constructed from a centered root mean square
envelope and a cubic Savitzky--Golay filter.  We repeat the extraction for
$54$ combinations of smoothing width, root-mean-square fraction, persistence
interval, and tolerance.  A setting resolves a transition only when it finds
both endpoints.  At the aggregate level, a case is called resolved when at
least $27$ settings do so and marginal when between $1$ and $26$ do so.  If
none resolves, the case is reported by the missing endpoint, such as ``no
cosmological entry.''  Quoted times are medians over the resolving settings,
with the full sweep range as a systematic interval.

At $r=8M$, the entry medians are $\kappa_cU=2.83$ for $L/M=80$ and $2.94$
for $L/M=160$, with sweep ranges $[2.56,3.07]$ and $[2.68,3.19]$.
They agree to $4\%$ in cosmological units while the corresponding geometric
times differ by a factor of $2.05$.  The departure medians, $115.9M$ and
$133.5M$, are instead approximately fixed in black-hole units; their
dimensionless ranges are $[1.15,1.76]$ and $[0.71,0.96]$ in $\kappa_cU$.
The transition interval consequently widens from approximately $117M$ to
$343M$.  With only two clean lengths, this contrast is consistency evidence
for the two physical scales and not a fitted scaling law.

The shorter-length cases show why the aggregate classification matters.  At
$L/M=20$ no observer enters a resolved cosmological regime.  At $L/M=40$ the
$r=4M$ case has no cosmological entry, the $r=8M$ and $r=16M$ cases are
marginal with respectively $1/54$ and $18/54$ resolving settings, while the
cosmological-horizon case is resolved at the threshold $27/54$.

The Schwarzschild reference does not settle within $5\%$ of its own
$\ell=1$ Price value until $U=220.3M$ at $r=8M$ and $167.3M$ at $\scri^+$,
both later than the corresponding finite-$L$ departures.  For $\ell=2$ the
Price value is not attained at $r=4M$ within the evolved interval.  Thus the
tested finite-$L$ solutions leave the Schwarzschild waveform before a clean
Schwarzschild power-law regime is established.  This supplies physical
context for the finite regulator interval; the production disjoint windows in
Sec.~\ref{sec:direct_recovery} are labeled diagnostic for the separate
numerical reason that their refinement change is not subdominant.

\section{Reproducibility record}
\label{app:reproducibility}

The frozen regulator campaign separates simulation from analysis at the
level of the repository history.  All $36$ raw archives, $21$ for the fixed
mode family and $15$ for the localized source family, were produced from the
single clean simulation commit \texttt{2460d976}.  Every regulator table
and figure was then generated at analysis commit \texttt{de3a3d5}, which does
not modify the raw archives.  The manifest records the full identifiers.  At
creation it requires all tracked analysis sources and external inputs outside
the output directory to be clean, and it rejects any archive whose embedded
provenance reports a dirty simulation worktree.

Each archive carries the exact case command that produced it, its refinement
level, its byte count, its SHA256 digest, and the digest of the physical
contract for its family.  The contract fixes $M=1$, the minimal gauge, the
compactification family, the height normalization at $r=4M$, the retarded
time $U=\tau-q_L$, and the initial datum or source, so a single hash
distinguishes a change of physics from a change of resolution.  Two contract
digests appear, one for each family.  Text artifacts are hashed after CRLF
and CR are canonicalized to LF, and their byte digests are retained as well,
so the record survives transfer between platforms.  Derived artifacts record
the raw inputs from which they were built, and the three external tables
taken from the earlier caustic package are listed as external inputs rather
than as products of this campaign.

The localized response signals are stored compactly as per-response
\texttt{int32} delta sequences, with a reconstruction scale chosen for a
maximum relative peak error of $10^{-7}$.  This storage scale is negligible
for the percent-level regulator comparisons.  The very small localized
medium-to-fine values in Table~\ref{tab:localized} are therefore used only to
establish that refinement is subdominant, not to resolve additional digits.

The foliation-conditioning data in Fig.~\ref{fig:foliation_limit}, the
crossover study of Appendix~\ref{app:latetime}, and the sourced cross-code
comparison are separate supporting data sets outside this production
manifest.  The foliation data and generator are retained under
\texttt{paper}; the crossover data are under
\texttt{results/sds\_scalar}, with their analysis implemented in
\texttt{black\_hole/crossover\_final.py}; and the cross-code archives remain
in the earlier caustic package.  This separation prevents the production
integrity record from being read as provenance for every supporting result in
the manuscript.

Regeneration and verification use the two recorded commands of the manifest.
Verification recomputes every digest and reports any mismatch by path, which
is how the presentation corrections to the figures and tables were checked
against the frozen raw archives.  No $L/M=1280$ simulation was
run, and the manifest records that fact explicitly so that the absence of a
next large-$L$ case cannot be mistaken for an omission from the archive.

The public result package separates frozen raw simulations from derived
analysis artifacts and records their cryptographic hashes and code
provenance.

\begin{acknowledgments}
A.Z. thanks Ted Jacobson, Rodrigo Panosso Macedo, and Alex
Va\~n\'o-Vi\~nuales for discussions.  This work was inspired by Charles
Misner's investigations of artificial cosmology and by the memorial
symposium held at the University of Maryland in 2023.  This material is
supported by NSF Grant No.~2309084.
\end{acknowledgments}

\section*{Data Availability}
The code and simulation data underlying the numerical results are publicly
available in
Ref.~\cite{blackhole_computation_2026}.  The manuscript source package
contains the additional scripts and local processed data used to assemble
the published figures.

\bibliographystyle{apsrev4-2}
\makeatletter
\immediate\write\@auxout{\string\citation{apsrev42Control}}
\makeatother
\bibliography{SdS_refs}

\end{document}
